spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/lab3_test}
    username: ${DB_USERNAME:lab3}
    password: ${DB_PASSWORD:lab3_test}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

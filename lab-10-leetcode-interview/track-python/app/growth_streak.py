"""Ticket I1 — Longest growth streak (Longest Increasing Subsequence pattern).

Given the daily volume of an operational metric, find the length of the
longest streak of days you could pick, in order, where the value strictly
increases. The days you pick do not need to be consecutive.
"""

from __future__ import annotations


def longest_growth_streak(volumes: list[int]) -> int:
    """Return the length of the longest strictly increasing subsequence."""
    raise NotImplementedError("Implement longest_growth_streak for ticket I1")

"""Ticket I2 — Longest peak-then-decline window (Longest Bitonic Subsequence
pattern).

Given a user's daily activity during a promotional campaign, find the length
of the longest streak of days you could pick, in order, that strictly
increases up to one peak and then strictly decreases. Either side of the
peak may be trivial (length one).
"""

from __future__ import annotations


def longest_peak_then_decline(activity: list[int]) -> int:
    """Return the length of the longest bitonic subsequence."""
    raise NotImplementedError("Implement longest_peak_then_decline for ticket I2")

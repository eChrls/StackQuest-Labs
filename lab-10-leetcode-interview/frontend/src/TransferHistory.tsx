import type {Transfer,TransferStatus} from './types';
export function TransferHistory({items,onTransition}:{items:Transfer[];onTransition:(id:number,status:TransferStatus)=>Promise<void>}){return <section><h2>Transfer history</h2>{items.length===0?<p>No transfers yet.</p>:<ul>{items.map(item=><li key={item.id}><b>#{item.id}</b> {item.userId} · €{Number(item.amount).toFixed(2)} · <span>{item.status}</span> <button onClick={()=>void onTransition(item.id,'COMPLETED')}>Complete</button><button onClick={()=>void onTransition(item.id,'PENDING')}>Reopen</button></li>)}</ul>}</section>}

import {useState} from 'react'; import type {NewTransfer} from './types';
export function TransferForm({onSubmit}:{onSubmit:(value:NewTransfer,key:string)=>Promise<void>}){const[userId,setUser]=useState('user-1'),[amount,setAmount]=useState('100.00'),[busy,setBusy]=useState(false),[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage('');try{await onSubmit({userId,amount:Number(amount)},crypto.randomUUID());setMessage('Transfer submitted')}catch(error){setMessage(error instanceof Error?error.message:'Unable to submit')}finally{setBusy(false)}}return <form onSubmit={submit}><label>User<input value={userId} onChange={e=>setUser(e.target.value)} required/></label><label>Amount<input type="number" min="0.01" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)} required/></label><button disabled={busy}>{busy?'Submitting…':'Send transfer'}</button><output>{message}</output></form>}

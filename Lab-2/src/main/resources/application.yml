spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/lab2}
    username: ${SPRING_DATASOURCE_USERNAME:lab2}
    password: ${SPRING_DATASOURCE_PASSWORD:lab2_dev}
  jpa:
    hibernate: {ddl-auto: validate}
    open-in-view: false
  flyway: {enabled: true}
server: {port: 8080}
logging:
  level: {com.example.lab2: INFO}

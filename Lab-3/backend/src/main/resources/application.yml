spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/lab3}
    username: ${DB_USERNAME:lab3}
    password: ${DB_PASSWORD:lab3_dev}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
server:
  port: ${SERVER_PORT:8080}

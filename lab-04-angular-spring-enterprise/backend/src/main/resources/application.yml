spring:
  datasource:
    url: "${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/lab4}"
    username: "${SPRING_DATASOURCE_USERNAME:lab4}"
    password: "${SPRING_DATASOURCE_PASSWORD:lab4_dev}"
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
